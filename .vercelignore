# Exclude large model files and training outputs
output/
*.safetensors
*.bin
*.pt
*.pth
checkpoint-*/

# Exclude Python cache and virtual environments
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/
ENV/

# Exclude data files (not needed for deployment)
*.jsonl
*.csv
*.txt
!requirements.txt

# Exclude fix scripts (not needed for deployment)
fix_*.py

# Exclude training script (not needed for deployment)
train_model.py

# Keep only essential files for web deployment
# index.html and kestra_flow.yaml should be included

